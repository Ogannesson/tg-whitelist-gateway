{
  "name": "tg-whitelist-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: IP registration gateway for tg-whitelist",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
